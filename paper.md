# 基于CTC的二合一吞音检测系统（生产级方案）

[TOC]



# 前言

​	在语音信号处理领域，发音质量评估（Pronunciation Assessment）一直是人机交互与语音康复分析的关键技术。其中，“吞音”（Swallowed Sound）现象——即说话过程中因发音不完整、音素压缩或口型不到位导致的音素丢失或模糊——是影响语音清晰度与可懂度的重要因素。传统的语音识别系统（ASR）通常旨在尽可能还原文本，往往会通过语言模型自动修正此类发音错误，从而掩盖了发音人的真实发音缺陷。因此，开发一套专用的吞音检测系统对于语音教学、临床构音障碍诊断及高质量语音合成数据清洗具有重要意义。针对上述问题，本文提出了一种基于连接时序分类（Connectionist Temporal Classification, CTC）架构的二合一吞音检测系统（2-in-1 Swallow Sound Detection System）。该系统创新性地构建了一个统一的分析框架，能够同时适配无参考文本（Reference-free）与有参考文本（Reference-based）两种应用模式。在无参考文本模式下，系统利用 Wav2Vec2 模型的 CTC 分支进行音素级分割，并结合能量、谱熵、基频等多模态声学特征计算吞音风险；在有参考文本模式下，进一步引入 CTC 强制对齐（Forced Alignment）机制，通过融合声学评分与对齐偏差评分，实现高精度的发音异常检测。本文的主要贡献在于提出了一种基于分段非线性映射的评分算法，有效增强了对严重吞音现象的区分度。

关于音频大模型相关知识背景，建议学习文章 -> https://huggingface.co/learn/audio-course/zh-CN/chapter1/introduction

音频检测通常可分为两大类：

第一类为无参考文本的场景，例如客服对话、半双工或全双工通话、即兴演讲以及临场指挥等业务场景；

第二类为有参考文本的场景，如朗读、演讲及会议记录等。

​   在不同类型的音频检测任务中，基于CTC（Connectionist Temporal Classification）架构的方法通常优于基于Seq2Seq（序列到序列）架构的方法。CTC采用仅包含编码器的架构，因而具有更高的分析速度，并且避免了Seq2Seq中交叉注意力机制可能引入的对无关声学特征的干扰，从而在声音检测与分析方面展现出天然的优势。然而，由于缺乏解码器，CTC在文本生成能力方面存在一定局限。为弥补这一不足，可结合额外的带时间戳的语音识别模型，以获取对应时间的文字输出，从而实现音频内容的精确对齐与分析。

该系统基于Wav2Vec2模型，通过CTC（Connectionist Temporal Classification）架构输出音素或字符的预测概率，并结合多模态声学特征计算每个音素的吞音风险评分。系统设计核心包括以下几个方面：

1. **特征归一化与加权**：系统使用多种声学特征，如音素时长、能量、谱熵、发声率、零交叉率（ZCR）、谱质心及基频变化等，通过归一化和线性加权形成初步评分。
2. **分段非线性映射**：为增强严重吞音的区分度，初步线性评分通过分段非线性函数（低分段放大、异常高分段指数衰减）映射到最终S2分数。
3. **可选优化**：提供ADMM（交替方向乘子法）接口，可在未来扩展中对无参考文本场景进行附加优化。但由于缺乏这类数据，并且推理实现有一定难度，未实现。
4. 若有参考文本，则累加声学检测识别叠加CTC强制对齐按预设比例（我设置是3：7）混合得出最后结果。
5. 对于以词为CTC强制对齐的，需要考虑用拼音音素还是token词为最小单位。Token（词）级对齐：语义明确，但易受同音词影响，例如“北京”可能被误对齐为“北经”。拼音/音素级对齐：可以降低同音词对齐错误，但多音字可能映射到不同拼音，需要通过上下文或语言模型解歧义。



# 一.无参考文本场景
​        对于无参考文本的音频检测任务，由于缺乏标准文本对照，分析主要依赖于声学特征的提取与处理。在设计识别流程时，可以将音频信号按照大约20毫秒的时间窗口进行切分，（这一时间尺度与人类发音一个音节或一个字符的持续时间相当）基于此划分，可以对每个音频片段进行特征提取，从而实现音素或字符级别的检测与分析。这种方法不仅能够捕捉短时声学变化，还为后续的音频内容识别与质量评估提供了可靠的基础。

``````bash
另外，在语言处理层面，不同语言的最小分析单位存在差异。对于英文，通常可以直接将单词或文本转换为对应的音标，从而以音素为基本分析单元进行处理。

而对于中文、日文和韩文等语言，其最小语音分析单位通常对应于单个字符。因此，在无参考文本的音频检测任务中，针对不同语言应选择合适的基本单元进行声学特征分析，以保证检测结果的精确性和可解释性。
``````

在缺乏参考文本的情况下，系统主要依赖声学特征进行音素级分析。检测流程如下：

1. **音频切分**：将输入音频以约20毫秒为窗口进行分帧，与人类发音一个音节或字符的持续时间相匹配。
2. **特征提取**：对每帧计算RMS能量、零交叉率、谱质心、谱带宽及基频等特征。
3. **CTC分段**：通过CTC模型的逐帧预测概率，将连续帧按最大后验token切分为音素段，同时合并过短段以减少噪声干扰。
4. **S2评分计算**：每个音素段根据线性加权特征计算初步S2值，再通过分段非线性映射得到最终风险评分。系统同时记录可能异常的原因，例如“音素过短”、“能量低”、“模糊发音”等。
5. **句级聚合**：将音素级S2分数结合90百分位和高风险比例进行非线性聚合，得到句级吞音评分及风险分层。

我使用了8个维度的声学特征以及基础的声学设置

对于输入语音信号 $x$，首先重采样至16kHz并分帧（帧长20ms）。系统提取一组多模态声学特征向量 $\mathbf{f} \in \mathbb{R}^8$，旨在捕捉吞音现象的物理表征（如时长压缩、能量衰减、频谱模糊等）。
定义第 $k$ 个音素段的声学风险评分 $S_{acoustic}^{(k)}$。首先计算线性加权和 $S_{linear}$：
$$
S_{linear}^{(k)} = \sum_{i=1}^{M} w_i \cdot \text{clamp}(\mathcal{N}(f_i^{(k)}))
$$

其中，$w_i$ 为特征权重（如时长权重 $w_{dur}=4.0$，能量权重 $w_{eng}=2.5$），$\mathcal{N}(\cdot)$ 为归一化函数，$\text{clamp}(\cdot)$ 将值限制在 $[0, 1]$ 区间。关键特征包括：

- Duration Penalty : $\mathcal{N} {dur} = (T {short} - T_{dur}) / T_{short}$，用于惩罚时长显著短于平均水平的音素。
- Posterior Confidence : $\mathcal{N} {prob} = 1 - \bar{p} {ctc}$，反映模型对当前音素识别的置信度，低置信度往往对应发音模糊。
- Spectral Entropy : $\mathcal{N} {ent} = H(p) / H {max}$，熵值越高表示频谱分布越无序，对应模糊发音。
- Energy & Voicing : 归一化的均方根能量（RMS）与发声率（Voicing Ratio），用于检测能量衰减与清浊音异常。

为解决线性评分在区分轻微与严重吞音时灵敏度不足的问题，本文设计了一种分段非线性映射函数 $\Phi(\cdot)$。该函数将线性评分 $S_{linear}$ 映射为最终的声学惩罚分数 $S_{acoustic}$：

$$
S_{acoustic} = \Phi(S_{linear}) = (1 - \sigma(\gamma \cdot (S_{linear} - \tau))) \cdot \mathcal{L}(S_{linear}) + \sigma(\gamma \cdot (S_{linear} - \tau)) \cdot \mathcal{H}(S_{linear})
$$

其中 $\sigma$ 为 Sigmoid 门控函数，$\tau$ 为切换阈值。$\mathcal{L}(\cdot)$ 与 $\mathcal{H}(\cdot)$ 分别为低分段与高分段的映射函数，旨在低风险区间放大轻微异常的信号，而在高风险区间通过指数衰减压制非特异性噪声，从而使得最终分数 $S_{acoustic}$ 能更准确地反映吞音的可能性（分数越低表示风险越高）。

```bash
duration: 惩罚音素过短的情况，吞音或吐字不清通常表现为音素被压缩，时长短于正常音素
bank: 惩罚静音片段，通常为静音片段被误判为音素
posterior: 惩罚模型对音素的低置信度
energy: 惩罚模型对音素的能量过低
entropy: 惩罚音素频谱不集中（模糊、不确定）
voicing: 惩罚低发声率音素
zcr: 惩罚频率变化过快的音素
spectral: 惩罚频谱偏高或偏低的音素

# 采样率：所有音频在处理前会被重采样到该采样率（Hz）
SAMPLE_RATE = 16000

# 每帧时长（毫秒），决定特征帧的 hop 长度和 CTC 帧率映射
FRAME_MS = 20.0

# CTC 模型中的 blank token ID（通常为 0），用于分割静音/填充
BLANK_ID = 0

# 合并非常短段的阈值（毫秒），小于该时长的段会合并到邻近段
MERGE_SHORT_MS = 10
```

但说实话，基于纯声学特征的吞音检测在实际应用中效果有限。一方面，由于缺乏足够的标注数据，难以有效利用ADMM等自适应优化方法对参数进行调优；另一方面，正常发音与轻微吞音的声学差异较小，严重依赖人工设定的阈值。由于阈值参数难以精确确定，无论采用分段指数下降还是增广拉格朗日，交替方向乘子法（ADMM）约束策略，音素级吞音的区分度仍然有限，难以获得理想的检测性能。

针对这一问题，在部分业务场景中可以将原本的无参考文本音频，通过构建或获取对应文本，将其转化为“有参考文本”场景，并利用CTC强制对齐方法进行音素或字符的对齐分析。实验证明，这种策略显著提升了吞音检测的准确性和鲁棒性，使得系统在语音质量评估和异常发音识别中表现更加可靠。 比如在音频质量识别场景，我们不单要依赖吞音检测，前置肯定有VAD，ASR，PUNC，ITN早都把音频转为文本了（甚至一些模型也附带了时间戳和说话人分离功能），那进一步我们可以采取手动纠正的方式进一步确保文本准确率。

# 二.有参考文本场景

在存在参考文本 $T_{ref}$ 的场景下，系统利用CTC强制对齐技术获取标准音素序列的时间边界。定义对齐风险评分 $S_{align}$，基于音素的压缩率（Compression Rate）与缺失情况（Deletion）计算：
最终的音素级惩罚分数 $S_{final}$ 采用基于概率互补的几何加权融合策略：
$$
S_{final} = 1 - (1 - S_{acoustic})^\alpha \cdot (1 - S_{align})^\beta
$$
其中，$\alpha$ 和 $\beta$ 分别为声学评分与对齐评分的置信度超参数（实验设定 $\alpha=0.2, \beta=0.8$）。该公式模拟了“正常发音”概率的联合分布，即只要声学特征或对齐结果任一显示高风险，最终得分即趋向于吞音判定。

主要步骤如下：
1.**音素或字符编码**：根据语言类型，将参考文本转换为音素序列或字符序列。例如，英文可直接转音标，中文、日文和韩文以字符为最小单位。

2.**CTC强制对齐**：利用模型输出的逐帧logits与参考序列进行CTC强制对齐，确定每个音素在时间上的对应帧区间。

3.**S1评分计算**：根据对齐结果识别音素的压缩或缺失情况，生成S1评分（文本对齐占比）。

4.**S2与S1融合**：结合声学S2评分与参考文本S1评分，通过加权公式得到最终音素级“惩罚分数”。同时，系统会更新原因列表，标注音素压缩或缺失等问题。

5.**句级聚合**：音素级最终分数经过非线性聚合得到句级吞音评分与风险等级。

那经过声学特征分析+CTC强制对齐我们就有了比较的效果。
在实现过程中，处理短时噪声与特征对齐是两个主要的技术难点。
首先，CTC 解码产生的原始片段可能包含极短的噪声帧（<10ms）。本文在 ctc_segments 方法中实现了动态合并策略，将过短片段归并至邻近的主音素段，有效降低了假阳性率。
其次，为了统一不同量纲的特征（如能量、过零率、时长），系统在 score_s2 方法中对所有特征进行了归一化处理（Clamping），并根据先验知识设定了 THRESH_DURATION 、 THRESH_ENERGY 等阈值，用于生成可解释的异常原因报告（如“音素过短”、“能量低”等）。


在使用espeak-ng时存在诸多问题，建议在linux下使用。由于phonemizer目前截至2026年1月12日仅支持以下后端检测，因此我们需要在linux下执行如下命令，把espeak-ng伪装成espeak，就不需要动phonemizer的源码了

```bash
espeak: 支持多种语言和国际音标（IPA）输出。
espeak-mbrola: 使用SAMPA拼音字母代替IPA，但不保留单词边界。
festival: 仅支持美式英语，使用自定义phoneset，允许在音节级别进行标记化。
segments: 一个Unicode分词器，构建从字素到音素映射的音素化，由用户作为文件提供。

# 查看espeak-ng地址
which espeak-ng
# 假设 espeak-ng 安装在 /usr/bin/espeak-ng
sudo ln -s /usr/bin/espeak-ng /usr/bin/espeak

# 设置huggingface仓库
vim ~/.bashrc
export HF_ENDPOINT=https://hf-mirror.com
export HF_HOME=/usr/local/mydata/hf
export HF_DATASETS_CACHE=/usr/local/mydata/hf/datasets
```

# 三.声学特征分析
为了归一化到[0,1]区间方便评分。我们可以使用sigmoid去做，但是很快发现问题，我们要做声学区分，那必然特征要拉开差距。但显然极值被压缩到0，1。特征值变得更为平滑，难以区分。因此我们可以采用switch，tanh,relu。以及admm等算法进行优化。但最终我还是选择了logSoftmax的exp，也就是softmax配合自定义分段权重法。理由如下：
- **Sigmoid**：
  - **原理**：经典的S型激活函数，将输入映射到 $(0,1)$。
  - **局限性**：存在**梯度消失**问题。在两端（极好或极差的信号）函数曲线过于平缓，导致严重吞音（Score $\approx 0$）和中度吞音（Score $\approx 0.2$）在数值上被压缩在一起，难以有效区分严重程度。

- **Swish** (或 Switch)：
  - **原理**：$f(x) = x \cdot \sigma(x)$，具备无上界、有下界、平滑非单调的特性。
  - **局限性**：输出范围无上界（Unbounded），不适合直接作为 $[0,1]$ 的概率性评分。若强行归一化，又会引入新的非线性失真，且其非单调性在评分系统中可能导致逻辑上的困惑（即某些区间内特征值增加反而分数下降）。

- **Tanh**：
  - **原理**：双曲正切函数，将输入映射到 $(-1, 1)$。
  - **局限性**：虽然解决了 Sigmoid 的非零中心化问题，但输出范围不符合概率定义（需线性变换）。且同样存在梯度饱和问题，在高风险区域的区分度依然不足。

- **ReLU**：
  - **原理**：线性整流单元 $f(x) = \max(0, x)$。
  - **局限性**：输出无上界，完全不适合作为归一化评分。若配合 `Clamp(0,1)` 使用，则在 $>1$ 的区域完全丢失梯度（信息丢失），无法区分“略微超标”和“严重超标”。

- **分段权重法 (Piecewise Nonlinear Mapping)** —— **最终选择方案**：
  - **原理**：结合了 Sigmoid 的平滑性与自定义权重的灵活性。通过设定阈值 $\tau$（如 0.5），将评分空间分为“低风险”和“高风险”两段。
    - **低分段**：使用 $1 - e^{-\alpha_1 x}$，放大轻微异常的信号，使其快速脱离 0 值区。
    - **高分段**：使用 $e^{-\alpha_2 (x - \tau)}$，对高风险特征进行指数级惩罚。
    - **平滑过渡**：利用 Sigmoid 生成门控系数（Gate），在两段之间进行平滑插值。
  - **优势**：
    1.  **高区分度**：在“疑似吞音”的临界点（Threshold）附近梯度最大，能够敏锐捕捉从正常到异常的突变。
    2.  **严厉惩罚**：对于确定的吞音行为，指数衰减确保其分数迅速逼近 0，与正常语音拉开显著差距。
    3.  **可解释性强**：可以通过调整 $\alpha_1, \alpha_2$ 参数直观地控制系统的“严厉程度”。
本文介绍的二合一吞音检测系统，实现了对有参考文本与无参考文本场景的统一分析框架。通过CTC模型结合多模态声学特征和参考文本对齐技术，系统能够在音素级精度上评估吞音风险，并通过非线性聚合生成句级评分，为语音质量监控、语言训练及人机交互场景提供可靠的分析工具。